[project]
name = "tools"
version = "0.1.0"
description = "Various tools and utilities"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "click",
    "google-genai>=1.0.0",
    "pillow",
    "pymongo",
    "python-dotenv",
    "reportlab",
    "tabulate",
    "tqdm",
]
